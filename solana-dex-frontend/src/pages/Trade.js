// src/pages/Trade.js

import React from 'react';
import Swap from '../components/Swap'; // Adjust the import path if necessary
import '../styles/styles.css'; // Ensure the path is correct

const Trade = () => {
  return (
    <main className="trade-page">
      <Swap />
    </main>
  );
};

export default Trade;
