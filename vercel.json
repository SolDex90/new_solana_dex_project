{
    "version": 2,
    "builds": [
      { "src": "package.json", "use": "@vercel/static-build" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/" }
    ],
    "static": {
      "directory": "build"
    },
    "buildCommand": "npm run build",
    "outputDirectory": "build"
  }
  